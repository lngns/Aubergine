module GroupTheoryTest where

import BordelFunctionLanguageTest

-- Firstly, we introduce "binary operation" and predicate to recognize it.
-- We will need a set : S. Acting on S have to be a closed operation.
